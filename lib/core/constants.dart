import 'package:flutter/material.dart';

const double horizontalPadding = 16.0;
const double verticalPadding = 12.0;

const EdgeInsetsGeometry screenPadding = EdgeInsets.fromLTRB(
  horizontalPadding,
  verticalPadding,
  horizontalPadding,
  0,
);

const double bottomSpacing = 0.05;
